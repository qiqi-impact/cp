#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vi = vector<int>;

int main() { 
    int n;
    cin >> n;
    cout << ((n>=4 && n%2==0) ? "YES" : "NO") << endl;
    return 0;
}